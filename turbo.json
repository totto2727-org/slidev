{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**", "!dist/pdf/**"]
    },
    "export": {
      "dependsOn": ["build"],
      "outputs": ["dist/pdf/**"]
    }
  }
}
